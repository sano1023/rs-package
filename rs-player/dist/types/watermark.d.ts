/** position をCSSクラスに解決する（未知は top-right） */
export function watermarkPositionClass(position: any): string;
/**
 * @param {import('./player.js').Player} player
 * @returns {HTMLElement}
 */
export function buildWatermark(player: import("./player.js").Player): HTMLElement;
