/** 直線 */
export const straight: {
    defaults: any;
};
/** 直交（水平垂直の折れ線） */
export const orthogonal: {
    defaults: any;
};
/** bezier（滑らかな曲線。toPath で曲線化する） */
export const bezier: {
    defaults: any;
};
/** avoid（障害物回避の直交。間にあるノードを避けて迂回する） */
export const avoid: {
    defaults: any;
};
/** 標準リンクタイプ一式 */
export const builtinLinkTypes: {
    defaults: any;
}[];
